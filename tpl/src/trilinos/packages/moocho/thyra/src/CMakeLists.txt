


SET(HEADERS "")
SET(SOURCES "")

SET(DIR ${CMAKE_CURRENT_SOURCE_DIR}/AbstractLinAlgPack)
INCLUDE_DIRECTORIES(${DIR})
APPEND_GLOB(HEADERS ${DIR}/*.hpp)
APPEND_GLOB(SOURCES ${DIR}/*.cpp)

SET(DIR ${CMAKE_CURRENT_SOURCE_DIR}/NLPInterfacePack)
INCLUDE_DIRECTORIES(${DIR})
APPEND_GLOB(HEADERS ${DIR}/*.hpp)
APPEND_GLOB(SOURCES ${DIR}/*.cpp)

SET(DIR ${CMAKE_CURRENT_SOURCE_DIR}/MoochoPack)
INCLUDE_DIRECTORIES(${DIR})
APPEND_GLOB(HEADERS ${DIR}/*.hpp)
APPEND_GLOB(SOURCES ${DIR}/*.cpp)

TRIBITS_ADD_LIBRARY(
  moochothyra
  HEADERS ${HEADERS}
  SOURCES ${SOURCES}
  DEPLIBS moocho
  )
