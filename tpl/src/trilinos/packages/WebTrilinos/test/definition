PACKAGE_NAME=WebTrilinos

(FRAMEWORK, INSTALL, WebTrilinosTests) {
    DIRS = ../c++/epetra,
           ../c++/amesos.
           ../c++/ifpack,
           ../c++/aztecoo,
           ../c++/ml;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL, WebTrilinosPyTests) {
    DIRS = ../python;
    COMM = SERIAL;
}
